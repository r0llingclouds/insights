# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Python project using Python 3.14. Managed with `pyproject.toml` (likely using `uv` or similar modern Python tooling given the `.python-version` file).

## Commands

```bash
# Run the application
python main.py

# Install dependencies (if using uv)
uv sync
```

## Architecture

Currently a minimal project with a single entry point in `main.py`.
